name = "spec-compliance"
description = "STUB 9.9.8 — 禁止 live 调度。Spec coverage 是一次多维 review 的第一维。"
sandbox_mode = "read-only"

developer_instructions = """
此角色已从 9.9.8 默认生命周期移除。不要 spawn spec-compliance。
"""
