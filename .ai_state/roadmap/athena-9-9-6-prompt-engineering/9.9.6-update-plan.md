---
version: "9.9.6"
revision: "v3.1-canonical"
status: "superseded-by-user-closure"
baseline: "9.9.3"
implementation_authorized: false
git_commit_authorized: false
---

# Athena 9.9.6 Canonical Update Plan

## Closure

2026-07-28 用户决定停止 9.9.6 prompt-engineering / gate-descaling 方向：原本的改动反复且叙述冗长，后续不再考虑同类扩展。已完成实现、同步、验证输出与历史会话保留；未执行的 H1/F1-F7、runtime-verify、正式 review、polish、architecture 与 release 不改写为 completed，本计划仅作为历史记录。

## Review target

9.9.6 已从不可变 9.9.3 模板生成 CC/CX 双端底稿，直接位于当前仓库：

```text
vibeCoding/
├── claude/9.9.6/
└── codex/9.9.6/
```

测试资产最终放在用户指定的 `vibeCoding/scripts/` 扁平结构，但本轮尚未创建，且不得提交 Git。

正式设计、AC 与四轮 critic disposition：

- `../../sprints/archive/2026/2026-07-25-athena-9-9-6-prompt-engineering/design.md`
- `../../sprints/archive/2026/2026-07-25-athena-9-9-6-prompt-engineering/checklist.yaml`
- `../../sprints/archive/2026/2026-07-25-athena-9-9-6-prompt-engineering/bottom-draft-evidence.md`

## Implemented in the bottom draft

### Claude Code

- 完整 fork 9.9.3 endpoint；版本刷为 9.9.6；
- target Claude Code 2.1.219+ / Opus 5；
- `model=best`，fallback 使用 `opus` / `sonnet` alias；
- architect/critic 保持 Fable；执行/审查角色从 Sonnet 升到 Opus；
- 删除 dated Opus/Sonnet pins 与旧 Sonnet 全局 override；恢复有效 9.9.3 配置并把 API timeout 更新到 600 秒；
- 主模型保持 `model=best`；不设全局 subagent override；architect/critic=Fable，其余五个 packaged agent=Opus；
- fresh `defaultMode=default`，迁移保留用户已有合法 mode；
- 保留 permissions、worktree、hooks、plugins，并提供默认不加载的本地代理 overlay；
- endpoint-local `pace/references/platform-contracts.md` 与 DRAFT release/migration 文档。

### Codex

- 完整 fork 9.9.3 endpoint；版本刷为 9.9.6；
- exact target Codex 0.145.0+ / GPT-5.6 Sol；
- built-in `model_provider="openai"`，覆盖 ChatGPT login 与 OpenAI API Key，迁移 preserve gateway/base URL；
- 保留 WSL acknowledgement、`[desktop]`、plugins、approval/sandbox、web search；
- 删除空 custom provider、手工 context/compact metadata、memories、stable feature 重复开关、手工 skill 注册、V1 depth/no-op agent fields；
- 保留 `[features.multi_agent_v2]` explicit opt-in 与 V2 字段；
- endpoint-local platform contract、DRAFT release/migration 文档，并保留完整历史 CHANGELOG。

### Shared semantic outcome

- 两端各有 26 skills；PACE 4 core + 5 conditional 保持；
- `.ai_state` 仍是唯一真相源；不新增 runtime capability state；
- 不创建 shared contracts、renderer、shared-skills、第二状态树或 tracked test tree；
- 根 `.gitignore` 只忽略 `/.claude/`，不会吞 release adapter。

## Verified

- System plan/design critic: Round 4 PASS，无 P0/P1/P2；
- CC 130 files / CX 133 files；9.9.3 零 diff；
- 2 JSON、10 TOML、25 Python parse PASS；17 Node hook syntax PASS；
- 26 skills/endpoint；
- CC main=`best`，无全局 subagent override；2×Fable + 5×Opus，角色 effort 保持 3×xhigh + 4×high；
- exact `codex-cli 0.145.0` 成功加载 package config；
- nested CC/CX adapter 可被 Git 发现；无 `.DS_Store`/cache；
- CX 历史 CHANGELOG suffix 与 9.9.3 byte-identical。

## Closed before final implementation

这是 reviewable bottom draft，不是 9.9.6 release candidate。以下原计划项在用户关闭方向后归档为 superseded，仅保留作历史范围说明：

1. CC/CX controlled skill invocation 的端内实现与行为测试；
2. PACE/hooks/gates 全量行为回归 —— **基线已刷新 (2026-07-28)**: gate-descaling + 小刀 + 下刀批次改了 manifest 必钉集 / critic 标题行计数 / checklist 可选 / P9 豁免 / re-route 数据源 / lite-admissible / token 采集降频, 回归期望集以 `.ai_state/harness-patches.md` W10-W24 为准, 不得拿旧契约当预期；
3. SessionStart ≤2500 bytes、breadcrumb ≤400 bytes 的 bounded injection；
4. snapshot/telemetry retention 实现与失败场景；
5. local-only N9/N10、migration/rollback tests；
6. ~~N≥3 prompt A/B 与 Pareto 结果~~ **已砍 (2026-07-28, 铁律[不过度工程]: 单用户无流量, 统计功效≈0)** → 替换为 dogfood 三指标: 下一业务 sprint tool-trace 代码写入占比 ≥40% · 手写 md 字节 ≤ 代码 diff 字节 · 零次人工拍板豁免门禁；
7. runtime-verify、2+1 review、polish、architecture、ship。

不得把底稿验证表述成上述项目已经完成，也不得在用户后续授权前 commit/push/release。

## Claude review focus

1. 两端配置是否符合 exact CC 2.1.219 / CX 0.145.0 合同；
2. 删除项是否误删真实用户/平台 surface；
3. Fable→Opus 是否还有活动 consumer；
4. OpenAI provider、App/WSL、API Key/gateway preserve 是否完整；
5. 当前底稿是否适合作为 F1–F7 的实现基线，而不是提前判定最终 release PASS。
